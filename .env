# ── URLs ──
FRONTEND_URL=http://localhost:3000

# ── MongoDB ──
MONGODB_URI=mongodb://localhost:27017/premisave-property

# ── Mail ──
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=thepeacemakerske@gmail.com
MAIL_PASSWORD="nyea rbvz wkzp ksaz"

# ── RabbitMQ ──
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# ── Redis ──
REDIS_HOST=localhost
REDIS_PORT=6379

# ── JWT ──
# Must be IDENTICAL to auth-service's JWT_SECRET — property-service only
# verifies tokens issued by auth-service, it never issues its own.
JWT_SECRET=eW91ci12ZXJ5LXNlY3VyZS1qd3Qtc2VjcmV0LWtleS0zMi1ieXRlcyE=

# ── External Microservices ──
AUTH_SERVICE_URL=http://localhost:8080
WALLET_SERVICE_URL=http://localhost:8084
LISTING_SERVICE_URL=http://localhost:8082
BOOKING_SERVICE_URL=http://localhost:8083

# ── Internal API Key ──
# Must be IDENTICAL to auth-service's API_KEY — this is the shared secret
# sent as X-API-Key on internal (Feign) calls between services.
# Generate with: openssl rand -hex 32
INTERNAL_API_KEY=00183e19c295366cf171c0d449d8b5ca46291573c9d1643e1e441b0bf571d1c7

# ── Notice delivery ──
NOTICE_EMAIL_FROM=no-reply@premisave.com

# ── Twilio (SMS notice delivery) ──
# Find these in the Twilio Console: https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_DEFAULT_COUNTRY_CODE=+254
