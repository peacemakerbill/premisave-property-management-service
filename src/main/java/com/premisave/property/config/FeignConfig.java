package com.premisave.property.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FeignConfig {
    // Feign client configuration (interceptors, error handling) can be added here
}